@model aspnetmvc_blog.Models.UserModule
@{
    ViewData["SelectedName"] = (ViewData["SelectedName"] != null) ? " for " + ViewData["SelectedName"] : "";
    ViewData["Title"] = "New Module " + ViewData["SelectedName"];
}
<form asp-action="Create">
    <h2>
        @ViewData["Title"]
        <input type="submit" value="Submit" class="btn btn-primary" />
        <a asp-action="Index" asp-route-id="@ViewData["SelectedId"]" class="btn btn-secondary">Back to List</a>
    </h2>
    <div class="row">
        <div class="col-md-4">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <div class="form-check">
                    <input class="form-check-input" id="checkbox1" asp-for="UserModuleAccess" checked />
                    <label class="form-check-label" for="checkbox1">
                        @Html.DisplayNameFor(model => model.UserModuleAccess)
                    </label>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="UserId" class="control-label"></label>
                <select asp-for="UserId" class="form-control" asp-items="ViewBag.UserId"></select>
            </div>
            <div class="form-group">
                <label asp-for="ModuleId" class="control-label"></label>
                <select asp-for="ModuleId" class="form-control" asp-items="ViewBag.ModuleId"></select>
            </div>
            <div class="form-group required">
                <label asp-for="UserModuleOrder" class="control-label"></label>
                <input asp-for="UserModuleOrder" class="form-control" required />
                <span asp-validation-for="UserModuleOrder" class="text-danger"></span>
            </div>
        </div>
    </div>
</form>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script language="JavaScript" type="text/javascript">
        $(document).ready(function () {
            $('#ModuleId').select2();
            $('#UserId').select2();
        });
    </script>
}
